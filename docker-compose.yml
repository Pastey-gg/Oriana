services:
  app:
    build:
      context: .
      args:
        VITE_API_HOST: ${VITE_API_HOST:-http://localhost:8080}
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 8181
    ports:
      - "${ORIANA_PORT:-8181}:8181"
