services:
  app:
    build:
      context: .
      args:
        VITE_API_HOST: ${VITE_API_HOST:-http://localhost:8080}
    restart: unless-stopped
    mem_limit: 256m
    ports:
      - "${ORIANA_PORT:-8181}:8181"
