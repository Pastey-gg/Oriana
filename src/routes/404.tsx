import ContentPage from "~/components/ContentPage";

export default function NotFound() {
  return (
    <ContentPage title="404" message="That paste does not exist or has been removed." titleColor="var(--accent-color)" />
  );
}
