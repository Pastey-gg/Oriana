import type { ParentComponent } from "solid-js";
import styles from "../styles/MetaBar.module.scss";

interface Props {
  paste: PasteResponse;
}

const MetaBarWith: ParentComponent<Props> = (props) => {
  return <div></div>;
};

export default MetaBarWith;
